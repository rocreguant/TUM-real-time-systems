#include <stdlib.h>
#include <unistd.h>
int main(){

    //TODOS use malloc function in a "for" loop and allocate memory for 1000, 10000,100000 and 1000000 integer values.
    //check which effects  do they have on OS memory with the "top" command.
   // What is the difference between virtual, resident and shared memory in Linux?  
    while(1){
      
       //TODO sleep here for 500, 250, 125,50 microseconds
       //and check with command "top" which effects this sleep time has.
    }
    return 0;
}


